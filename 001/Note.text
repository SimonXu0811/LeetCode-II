两数之和就是从数组中找到两个目标值
可以利用双指针的方法，从头尾开始
如果两个值相加正好是目标值就返回
如果相加大于目标值，就右指针向左移动
如果小于，就左指针向右移动
原地算法节省了内存
空间复杂度为:O(1)
时间复杂度为:O(nlogn)

如果需要改进那么就放弃空间，利用set查找，一次循环保存进set中
然后遍历set中元素，查找有没有另一个目标值匹配
空间复杂度为:O(n)
时间复杂度为:O(n)
